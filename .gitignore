# ── 项目文档与本机指令(不随仓库发布)──────────────────
docs/
CLAUDE.md
CLAUDE.local.md

# ── 本地工具状态 / 缓存 / 生成物 ───────────────────────
.code-graph/
.claude/
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
.ruff_cache/

# ── 密钥 / 环境 / 本地配置 ─────────────────────────────
.env
.env.*
!.env.example
/config.yaml

# ── Python 虚拟环境 / 打包产物 ─────────────────────────
.venv/
venv/
env/
*.egg-info/
build/
dist/
.python-version

# ── 编辑器 / 操作系统 ─────────────────────────────────
.vscode/
.idea/
*.sw[op]
.DS_Store
Thumbs.db
*.log
node_modules/

# ── 运行产物:逐委员意见与统计可再生,不入库 ───────────
# 保留 COST-NOTE.md / brief.md / config.yaml 作可复现证据样例(见 moa-reports/cost-m4/)。
moa-reports/**/member_*.json
moa-reports/**/stats*.json
moa-reports/**/discussion.jsonl
moa-reports/**/blindvote_*.json
moa-reports/**/discuss_stats.json
